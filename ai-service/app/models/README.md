# AI Models Directory
This directory contains serialized machine learning models (e.g., .joblib or .pkl files).

Currently, models are initialized with mock data on startup in `app/services/`. 
In production, run the training scripts to populate this folder.
